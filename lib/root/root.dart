import 'package:flutter/material.dart';
import 'package:orbit/Widgets/bottom_nav_bar.dart';

class Root extends StatelessWidget {
  const Root({super.key});

  @override
  Widget build(BuildContext context) {
    return BottomNavBar();
  }
}
